# rclone_script
Simple bash script to automate sync on cloud services from Terminal

rclone_script2 is a simple script written in bash that connect to my serivces via rclone.
It also copies some Steam screenshots and Stability Matrix images to specific folders, and backups the "bash" directory in "Documenti".
It's wirtten for my needs, that's why the repo is private.
